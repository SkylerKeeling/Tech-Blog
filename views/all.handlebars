{{#each post}}
  <div class="post-card">
    <a href="/{{post_id}}"></a>
    <h2>{{user.username}}</h2>
    <div class="post-card-header">
      <h1>{{title}}</h1>
    </div>
    <div class="card-body">
      <p>
        {{caption}}
      </p>
    </div>
  </div>
{{/each}}